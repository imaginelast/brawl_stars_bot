# bs_object_detection

requirements:
Use bluestacks for the brawl stars, change code depending on size of the window for accuracy (when bot using super/gadget)
Custom empty bounty map, make it decent size, no bs bots other than player and code bot
Make sure bot using frank, best brawler for this occasion

other:
bot really cheeks and moving forward code might not work so make sure to manually move the bot forward until it sees opponent
sometimes bot doesnt accurately find enemy so either manually move the both or move enemy to a different location
to make it more challenging, enemy should use bad brawlers 
